{
  "schema": "1.0.0",
  "type": "adapter",
  "version": "1.0.0",
  "configs": [
    {
      "displayName": "String config value",
      "description": "The example of String type config value.",
      "type": "string",
      "name": "ADAPTER_CUSTOM_STRING",
      "required": false,
      "defaultValue": "test"
    },
    {
      "displayName": "Number config value",
      "description": "The example of Number type config value.",
      "type": "number",
      "name": "ADAPTER_CUSTOM_NUMBER",
      "required": false,
      "defaultValue": "123"
    },
    {
      "displayName": "Boolean config value",
      "description": "The example of Boolean type config value.",
      "type": "boolean",
      "name": "ADAPTER_CUSTOM_BOOLEAN",
      "required": false,
      "defaultValue": "true"
    },
    {
      "displayName": "RegExp config value",
      "description": "The example of RegExp type config value.",
      "type": "regexp",
      "name": "ADAPTER_CUSTOM_REGEXP",
      "required": false,
      "defaultValue": "^.*$"
    },
    {
      "displayName": "Multiline config value",
      "description": "The example of Multiline type config value.",
      "type": "multiline",
      "name": "ADAPTER_CUSTOM_MULTILINE",
      "required": false,
      "defaultValue": "test"
    },
    {
      "displayName": "JSON config value",
      "description": "The example of JSON type config value.",
      "type": "json",
      "name": "ADAPTER_CUSTOM_JSON",
      "required": false,
      "defaultValue": "{ \"key\": \"value\" }"
    },
    {
      "displayName": "Comma separated list config value",
      "description": "The example of Comma separated list type config value.",
      "type": "commaSeparatedList",
      "name": "ADAPTER_CUSTOM_COMMA_SEPARATED_LIST",
      "required": false,
      "defaultValue": "foo,bar"
    },
    {
      "displayName": "Select config value",
      "description": "The example of Select type config value.",
      "type": "select",
      "enum": [
        "foo",
        "bar"
      ],
      "name": "ADAPTER_CUSTOM_SELECT",
      "required": false,
      "defaultValue": "foo"
    }
  ]
}